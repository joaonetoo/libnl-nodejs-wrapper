{
  "name": "n-api-libnl",
  "version": "0.1.0",
  "description": "libnl addon for nodejs",
  "main": "module.js",
  "scripts": {
    "start": "node-gyp configure build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "engines": {
    "node": "10.16.0"
  },
  "license": "MIT",
  "dependencies": {
    "ip-to-int": "^0.3.1"
  },
  "devDependencies": {
    "electron-rebuild": "^1.8.5"
  }
}
